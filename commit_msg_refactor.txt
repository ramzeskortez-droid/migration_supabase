ВЕРСИЯ 1.18.56 - Глубокий рефакторинг OperatorInterface и фиксы производительности
- Полная декомпозиция OperatorInterface.tsx (500+ строк -> 115 строк):
  - Вынесен хук useOperatorAuth.ts для логики авторизации.
  - Создан компонент OperatorOrderCreation(СозданиеЗаявки).tsx (форма, файлы, запчасти, AI).
  - Создан компонент OperatorOrdersView(ПросмотрЗаявок).tsx (поиск, фильтры, список).
- Оптимизация поиска: внедрен Debounce (300мс) для предотвращения лишних запросов к API при вводе.
- Исправлена ошибка React: "Cannot update a component while rendering a different component" при переходе из GlobalChatWindow (вызов обернут в setTimeout).

БЫЛА ПРОБЛЕМА:
1. OperatorInterface.tsx стал "God Object", сложен для поддержки и расширения.
2. Поиск по заказам "тормозил" из-за отсутствия задержки ввода (запросы летели на каждый символ).
3. Ошибки в консоли при навигации из чата к заказу.

КАК РЕШИЛИ:
1. Разделение ответственности (UI vs Logic).
2. Вынос стейта формы и списков в изолированные компоненты.
3. Оптимизация реактивности (useEffect debounce, async state updates).