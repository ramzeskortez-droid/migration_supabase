# Version History

## 1.18.44 - Стабилизация загрузки файлов
- **Operator UI:** Улучшена логика сохранения файлов в `PartsList`. Теперь массив файлов гарантированно сохраняется в стейт перед отправкой.
- **Fix:** Устранена причина потери файлов при создании заказа (зависимость от `photo_url` вместо `item_files`).

## 1.18.43 - Улучшение отображения файлов
...