# Version History

## 1.18.45 - Фикс передачи файлов при создании
- **Operator Interface:** Исправлена ошибка в `handleCreateOrder`, из-за которой массив файлов `itemFiles` не передавался в сервис создания заказа. Это приводило к потере всех файлов, кроме первого (photoUrl).

## 1.18.44 - Стабилизация загрузки файлов
...