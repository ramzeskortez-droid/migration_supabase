# Tracks

## Текущий трек: Виртуализация списков

**Цель:** Обеспечить плавную работу списков заказов при большом количестве данных.

**Выполнено:**
- Установлена библиотека `react-virtuoso`.
- Переработан `AdminOrdersList.tsx`: внедрен виртуальный список и мемоизация строк (`AdminOrderRow`).
- Переработан `SellerOrdersList.tsx`: внедрен виртуальный список и мемоизация строк.
- Удалена устаревшая логика пагинации (теперь рендерится весь доступный массив, но только видимая часть).

**Статус:** Завершено. Готов к внедрению серверного поиска и ленивой загрузки.
