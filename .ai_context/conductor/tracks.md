# Tracks

## Текущий статус: Завершено (Эпик: Финансы, Роли и Закупщик 2.0)

---

## Завершенный трек: Финансы, Изоляция и Закупщик 2.0
**Цель:** Реализовать ролевую модель (Менеджер/Оператор/Закупщик), управление курсами валют, изоляцию данных и новый интерфейс закупщика.

**Выполнено:**

1. **База данных & SQL:**
   - [x] Создание таблиц `app_users` (с телефоном), `exchange_rates`, `buyer_order_labels`.
   - [x] Модификация `orders` (owner_token) и `order_items` (admin_price_rub, calculated_price).
   - [x] Миграции: `30_epic_finance_roles_isolation.sql`, `31_fix_offer_items.sql`, `32_add_phone_to_users.sql`.

2. **Бэкенд (Service Layer):**
   - [x] Реализован `loginWithToken` и `registerUser` (с инвайт-кодом `china-nai`).
   - [x] Методы для работы с курсами (get/upsert) и авторасчет цены.
   - [x] Логика изоляции: оператор видит только свои заказы (`owner_token`).
   - [x] Защита от дублей офферов (один поставщик = один оффер).

3. **Интерфейс Менеджера (ex-Админ):**
   - [x] Полный ребрендинг: "Админ" -> "Менеджер".
   - [x] Страница "Курсы и расчеты" с авторасчетом цены (Курс * Цена + Вес * Доставка).
   - [x] Переработанный листинг: новые колонки (Тема, Бренд, Статистика), удалены лишние (VIN, Год).
   - [x] Кнопка "Выбрать лидера" с логикой toggle (вкл/выкл) и мгновенным обновлением UI.
   - [x] Мгновенные обновления списка (polling 5s).

4. **Интерфейс Оператора:**
   - [x] Строгая валидация бренда: кнопка "Создать" блокируется, если бренда нет в базе.
   - [x] Кнопка "Обработано" для быстрого перевода в "КП готово".
   - [x] Чат с закупщиками перенесен к оператору.
   - [x] Отображение итоговой цены (admin_price_rub) в списке позиций.

5. **Интерфейс Закупщика (ex-Поставщик):**
   - [x] Ребрендинг роутов (`/buyer`) и UI.
   - [x] Новая авторизация: Вход по токену, Регистрация.
   - [x] Система стикеров (цветные метки на заказы).
   - [x] Таб "Горящие 🔥" (заказы > 3 дней без офферов).
   - [x] Умный фильтр брендов и фильтрация списка (исчезновение обработанных из "Новых").
   - [x] Чат: отображение имени конкретного оператора.

---

## Завершенный трек: Интерфейс Оператора 2.0 и ИИ (Январь 2026)

**Цель:** Создать современный, быстрый интерфейс для создания заявок с использованием ИИ.

**Выполнено:**
- **Архитектура:** Создан новый модуль `components/operator` (Header, OrdersList, OrderInfo, PartsList).
- **ИИ-Ассистент:** Интегрирован Groq API для парсинга текста заявки (выделение позиций, контактов, темы письма).
- **Работа с брендами:**
  - Создана таблица `brands` (3000+ записей).
  - Реализован **нечеткий поиск** (Fuzzy search) при вводе бренда.
  - Подсветка неизвестных брендов и быстрое добавление в базу.
- **UI/UX:**
  - Табличный листинг заказов в стиле Поставщика (Virtualization).
  - Бесконечный скролл (Infinite Scroll) с подгрузкой данных.
  - Мгновенная отправка сообщений в чате (Optimistic UI).
  - Валидация полей ввода.

---

## Завершенный трек: Виртуализация списков

**Цель:** Обеспечить плавную работу списков заказов при большом количестве данных.

**Выполнено:**
- Установлена библиотека `react-virtuoso`.
- Переработан `AdminOrdersList.tsx` и `SellerOrdersList.tsx`.
- Внедрен виртуальный скроллинг и мемоизация строк.

---

## Бэклог (Идеи на будущее)
- [ ] Серверная пагинация для админки (сейчас грузится всё/много).
- [ ] Улучшение парсинга ИИ (обучение на примерах).
- [ ] Ролевая модель (защита роутов на сервере/клиенте - RLS уже внедрен, но можно усилить).
- [ ] Уведомления в Telegram для операторов о новых сообщениях.