-- Исправление бренда Makita (ID 3102) на эталонную латиницу
-- Это устранит проблемы, если бренд был записан кириллицей или со спецсимволами.
UPDATE brands 
SET name = 'Makita' 
WHERE id = 3102;

-- Дополнительно: убираем пробелы у всех брендов
UPDATE brands
SET name = TRIM(name);