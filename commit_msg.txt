ВЕРСИЯ 1.18.55 - Рефакторинг OperatorOrderRow и Фикс файлов
- Компонент OperatorOrderRow.tsx разбит на OperatorClientInfo(ИнформацияОКлиенте).tsx и OperatorOrderItems(СписокТоваров).tsx.
- Внедрен сводный реестр всех файлов заказа в карточке клиента.
- Новая логика иконок файлов: превью только при 1 фото, иначе иконка стопки с общим счетчиком.
- Исправлено обрезание тултипов файлов в таблицах Оператора и Менеджера.
- Исправлен баг потери фото при создании заказа.

БЫЛА ПРОБЛЕМА:
1. Тяжелый компонент строки заказа.
2. Неудобный просмотр вложений.
3. Ошибки верстки тултипов (z-index/overflow).
4. Потеря данных (photoUrl vs itemFiles) при создании заказа.

КАК РЕШИЛИ:
1. Декомпозиция React компонентов.
2. Агрегация всех файлов в OperatorClientInfo.
3. CSS фиксы верстки (z-index: 999, удаление overflow-hidden).
4. Явная инициализация itemFiles из photoUrl в OperatorInterface.